# Vercel Deployment

Deploy the web app from `apps/web`.

Recommended environment variables:

```txt
NEXT_PUBLIC_API_URL=https://your-api-host.example.com
```

The app can still demo locally without the API because the frontend includes a deterministic architecture engine fallback.

